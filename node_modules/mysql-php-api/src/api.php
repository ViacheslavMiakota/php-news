<?php

  // Load configuration
  $config = json_decode(file_get_contents('api.config.json'), true);

  // Connect to the database
  $pdo = new PDO('mysql:host=' . $config['host'] . ';dbname=' . $config['database'], $config['username'], $config['password']);

  // Output
  echo 'API is running ...'

?>